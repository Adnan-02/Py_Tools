#Celsius == Fahrenheit

x = int(input('For Celsius input type 1: \nFor Fahrenhiet input type 2:\n'))

if x == 1:
 C = float(input('Enter temp in Celsius:'))
 F = ( C*1.8 ) + 32
 print(C, 'Degrees Celsius is equal to', round(F, 3), 'Degrees Fahrenheit.')

elif x == 2:
    F = float(input('Enter temp in Fahrenheit:'))
    C = ( F-32 ) / 1.8
    print(F, 'Degrees Fahrenheit is equal to', round(C, 3), 'Degrees Celsius.')
